using UnityEngine;
using System.Collections;

public class TESTSCRIPT : MonoBehaviour {

	// Use this for initialization
	void Start () {
//		for (int j = 0; j < typeof(BuildingDefinition).GetFields().Length; j++) {
//			System.Reflection.FieldInfo prop = typeof(BuildingDefinition).GetFields()[j];
//			Debug.Log("" + prop.Name);
//		}


//		Debug.Log("EnemyLibrary.I.Get(EnemyType.BIKE);: " +EnemyLibrary.I.GetDefinition(EnemyType.BIKE));
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
